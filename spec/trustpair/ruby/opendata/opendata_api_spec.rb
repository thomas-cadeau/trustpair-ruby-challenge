require 'rspec'

describe 'The opendata api' do

  it 'should return a record when searching company\'s information by a valid siret number' do
    true.should == false
  end

  it 'should not return a record when searching company\'s information by an invalid siret number' do
    true.should == false
  end
end