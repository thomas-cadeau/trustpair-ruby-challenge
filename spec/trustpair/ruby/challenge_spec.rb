RSpec.describe Trustpair::Ruby::Challenge do
  it "has a version number" do
    expect(Trustpair::Ruby::Challenge::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
